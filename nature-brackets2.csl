<?xml version="1.0" encoding="utf-8"?>
<style xmlns="http://purl.org/net/xbiblio/csl"
       class="in-text"
       version="1.0"
       demote-non-dropping-particle="sort-only"
       default-locale="en-GB">

  <info>
    <title>Nature (brackets, IJPEMGT – full authors, no et al.)</title>
    <id>http://www.zotero.org/styles/nature-brackets-ijpemgt-final</id>
    <link href="http://www.zotero.org/styles/nature-brackets" rel="self"/>
    <category citation-format="numeric"/>
    <category field="engineering"/>
    <updated>2025-12-17T00:00:00+00:00</updated>
    <rights license="http://creativecommons.org/licenses/by-sa/3.0/"/>
  </info>

  <!-- ===== AUTHOR FORMAT: Surname, Initials, & Last Author (NO ET AL) ===== -->
  <macro name="author">
    <names variable="author">
      <name name-as-sort-order="first"
            initialize-with=". "
            delimiter=", "
            and="symbol"
            delimiter-precedes-last="always"/>
    </names>
  </macro>

  <!-- ===== TITLE ===== -->
  <macro name="title">
    <text variable="title"/>
  </macro>

  <!-- ===== JOURNAL ===== -->
  <macro name="container-title">
    <text variable="container-title" font-style="italic"/>
  </macro>

  <!-- ===== VOLUME ===== -->
  <macro name="volume">
    <text variable="volume"/>
  </macro>

  <!-- ===== DOI ===== -->
  <macro name="access">
    <choose>
      <if variable="DOI">
        <text variable="DOI" prefix="https://doi.org/"/>
      </if>
    </choose>
  </macro>

  <!-- ===== IN-TEXT CITATION [1–4] ===== -->
  <citation collapse="citation-number">
    <sort>
      <key variable="citation-number"/>
    </sort>
    <layout prefix="[" suffix="]" delimiter=", ">
      <text variable="citation-number"/>
    </layout>
  </citation>

  <!-- ===== BIBLIOGRAPHY ===== -->
  <bibliography et-al-min="999"
                et-al-use-first="999"
                second-field-align="flush"
                entry-spacing="0"
                line-spacing="2">

    <layout suffix=".">
      <text variable="citation-number" suffix="."/>
      <group delimiter=" ">

        <!-- Authors -->
        <text macro="author"/>

        <!-- Year -->
        <date variable="issued">
          <date-part name="year" prefix=" (" suffix=")."/>
        </date>

        <!-- Title -->
        <text macro="title" suffix="."/>

        <!-- Journal -->
        <text macro="container-title"/>

        <!-- Volume -->
        <text macro="volume" prefix=", "/>

        <!-- Pages -->
        <text variable="page" prefix=", "/>

        <!-- DOI -->
        <text macro="access" prefix=". "/>

      </group>
    </layout>
  </bibliography>

</style>
